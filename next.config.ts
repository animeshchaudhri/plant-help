import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   images: {
    domains: ['blogger.googleusercontent.com', 'res.cloudinary.com', 'images.unsplash.com', 'cdn.pixabay.com', 'images.pexels.com', 'www.pexels.com', 'cdn.pixabay.com', 'images.unsplash.com', 'images.pexels.com'
    ]
  }
};

export default nextConfig;
